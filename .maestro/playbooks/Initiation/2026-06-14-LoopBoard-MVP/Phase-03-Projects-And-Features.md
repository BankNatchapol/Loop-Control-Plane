# Phase 03: Projects And Features

This phase adds the project manager and feature/spec artifact manager so LoopBoard can point at real local repositories and understand PRD, spec, plan, task, and decision files. The result is a usable local project workspace instead of a single seeded demo board.

## Tasks

- [x] Inspect current persistence, UI, and filesystem helpers before extending them:
  - Search for existing project, feature, file, shell, and API patterns and reuse them
  - Keep all local filesystem access server-side
  - Preserve the working Phase 01/02 board while adding project selection and artifact awareness
  - Completion note: Inspected the current Next.js App Router app, SQLite migration/schema, repository layer, API route helpers, browser API client, task context filesystem service, page-level project/task UI, and focused tests before extending Phase 03. Existing reusable patterns are `LoopBoardRepository` for DB validation/transactions, `withLoopBoardRepository` plus `jsonOk`/`handleApiError` for Node-only API routes, `LoopBoardApiError`/client helpers for browser calls, and `TaskContextService` for server-side filesystem writes/status checks under configured roots. The current persistence model already seeds and lists projects/features/tasks, but only task mutation APIs exist; project and feature management should extend the repository/API pattern rather than bypass it. Local filesystem access is currently server-side in migrations and `lib/context/task-context-service.ts`; future repo/artifact discovery/open actions should stay in new server-only services/routes with fixed commands and validated paths. The working Phase 01/02 board is preserved through `/api/board?projectId=...`, localStorage-selected project/task IDs, feature chips, persisted Kanban drag/actions, and task context controls. Verified baseline with `npm test` and `npm run typecheck`. Analyzed 0 task images; none were associated with this checkbox.

- [x] Implement local project management:
  - Add create/edit/list/delete project flows using the `Project` schema from the PRD
  - Validate that `repoPath` exists and detect whether it is a Git repository
  - Detect current branch, default branch when possible, and GitHub remote URL if available
  - Store configurable paths for specs, tasks, workflows, and handoffs
  - Add UI to select the active project and show repository health details
  - Completion note: Expanded the local `Project` model and SQLite persistence with `repoPath`, Git repository status, current/default branch, GitHub remote URL, and configurable specs/tasks/workflows/handoffs paths. Added migration `0002_project_local_management.sql`, idempotent seed updates, repository create/update/list/delete methods, server-only repository health inspection via fixed `git` commands, `/api/projects` CRUD routes with `repoPath` existence/directory validation, and browser client helpers. Added header UI for active project selection, repository health details, create/edit/delete project flows, and path configuration while preserving the existing board/task interactions. Covered project CRUD, GitHub remote parsing, Git metadata detection, and migration expectations with focused tests. Verified with `npm test`, `npm run typecheck`, and `npm run lint`. Skipped commit/push because `/Users/bank.p/Documents/Loop-Control-Plane` is not a Git repository. Analyzed 0 task images; none were associated with this checkbox.

- [x] Add local open actions for project folders:
  - Implement safe server-side actions or API routes to reveal a project in the file explorer and open it in VS Code
  - Check for command availability and return clear errors if `code` is not installed
  - Never execute arbitrary user-provided shell commands; only run fixed commands with validated local paths
  - Add compact buttons in the project UI for Open Folder and Open VS Code
  - Completion note: Added a server-only project open action service that revalidates the stored project `repoPath` as an existing local directory before launching fixed file explorer or `code` commands with `shell: false`. Added `/api/projects/[projectId]/open` for `open-folder` and `open-vscode`, integrated `ProjectOpenActionError` into API error handling, and added browser client support. Wired compact Open Folder and Open VS Code buttons into the project toolbar with existing project feedback messaging; VS Code requests now return a clear `command_unavailable` error when the `code` CLI is not on PATH. Added focused tests for fixed command selection, missing/non-directory path rejection, missing `code` handling, and client endpoint wiring. Verified with `npm test`, `npm run typecheck`, `npm run lint`, and a local Next HTTP 200 check on `http://localhost:3000`. A hydrated in-app Browser check was skipped because the Browser plugin's required Node REPL control tool was not exposed in this session. Skipped commit/push because `/Users/bank.p/Documents/Loop-Control-Plane` is not a Git repository. Analyzed 0 task images; none were associated with this checkbox.

- [x] Implement feature/spec artifact management:
  - Add create/edit/list feature flows using the `Feature` schema from the PRD
  - Link existing Spec Kit output folders and detect `PRD.md`, `spec.md`, `plan.md`, `tasks.md`, and `decisions.md`
  - Add feature statuses for PRD draft, spec review/approved, plan review/approved, tasks ready, in execution, and done
  - Show feature artifact completeness and approval status on the board and feature pages
  - Completion note: Expanded the feature domain model, SQLite schema, seed data, repository layer, API client, and API routes for PRD-aligned feature statuses plus linked artifact folders and tracked `PRD.md`, `spec.md`, `plan.md`, `tasks.md`, and `decisions.md` paths. Added server-side artifact discovery in `lib/features/feature-artifacts.ts` that validates linked folders under the project repository/configured spec root, detects current file existence, and derives approval state from the feature lifecycle status. Added `/api/features` create/list and `/api/features/[featureId]` read/update/delete routes, refreshed `/api/board` responses with current artifact completeness, and added UI controls for selecting, creating, editing, deleting, and inspecting features. Board cards and task details now show linked feature status and artifact completeness, while the feature panel shows per-file found/missing and approval state. Added focused tests for artifact discovery, feature repository CRUD, migration expectations, and browser client endpoint wiring. Verified with `npm test`, `npm run typecheck`, `npm run lint`, `npm run db:migrate`, and a local Next smoke test against `GET /` and `GET /api/board` on `http://127.0.0.1:3100`. A hydrated in-app Browser check was skipped because the Browser plugin's required Node REPL control tool was not exposed in this session. Skipped commit/push because `/Users/bank.p/Documents/Loop-Control-Plane` is not a Git repository. Analyzed 0 task images; none were associated with this checkbox.

- [x] Build artifact viewer and editor:
  - Add markdown viewing for PRD, spec, plan, tasks, and decisions files
  - Add simple markdown editing with save/reload actions for linked local files
  - Protect against path traversal by requiring artifact paths to stay inside the configured project/spec roots
  - Show file missing, read error, and unsaved-change states clearly
  - Completion note: Added a server-only feature artifact document service and `/api/features/[featureId]/artifacts/[artifactName]` route for reading and saving linked PRD, spec, plan, tasks, and decisions markdown files. The service validates artifact names and requires resolved paths to stay inside the selected project's repository or configured spec root before reading or writing, returning explicit missing-file, read-error, and write-error states. Added browser client helpers and a feature-panel artifact viewer/editor with artifact tabs, markdown preview, textarea editing, save/reload actions, missing-file badges, API error messages, and unsaved-change state. Added focused tests for artifact document reads, missing-file state, saves, path traversal rejection, and client endpoint wiring. Verified with `npm test`, `npm run typecheck`, and `npm run lint`. Skipped commit/push because `/Users/bank.p/Documents/Loop-Control-Plane` is not a Git repository. Analyzed 0 task images; none were associated with this checkbox.

- [x] Add approval events for feature artifacts:
  - Implement Mark Spec Approved, Mark Plan Approved, and Mark Tasks Approved actions
  - Update feature status and append `SPEC_APPROVED`, `PLAN_APPROVED`, or `TASKS_APPROVED` events where relevant
  - Reflect approval status on linked tasks and board cards
  - Keep approval actions manual and explicit
  - Completion note: Added explicit feature approval event types and a durable `feature_events` SQLite table with migration `0004_feature_approval_events.sql`. Implemented repository-level `approveFeatureArtifact` behavior for spec, plan, and tasks approvals that advances feature status without downgrading later statuses and appends `SPEC_APPROVED`, `PLAN_APPROVED`, or `TASKS_APPROVED` events only when the approval changes state. Added `/api/features/[featureId]/approvals`, browser client support, feature panel Mark Spec/Plan/Tasks Approved buttons, recent feature event display, and approval counts on feature/task board completeness indicators so linked task cards reflect artifact approval status through their feature metadata. Added focused repository, migration, and client tests. Verified with `npm test`, `npm run typecheck`, and `npm run lint`. Skipped commit/push because `/Users/bank.p/Documents/Loop-Control-Plane` is not a Git repository. Analyzed 0 task images; none were associated with this checkbox.

- [x] Test project and feature workflows:
  - Write focused tests for Git detection, GitHub remote parsing, path validation, artifact discovery, and feature status transitions
  - Test UI flows for adding a project, linking a feature folder, viewing/editing artifacts, and approving artifacts
  - Run lint, type checks, and the relevant test suite, then fix failures
  - Completion note: Expanded focused workflow coverage for Phase 03 project and feature behavior. Added GitHub remote normalization and non-Git folder health tests, stricter artifact discovery validation for file paths, and a repository regression test ensuring manual approvals do not downgrade later feature statuses. Added browser API client workflow tests that cover adding/editing/deleting a project, listing/linking/updating a feature folder, viewing and saving artifact markdown, approving an artifact, and deleting the feature through the same stable endpoints used by the UI. Verified with `npm test`, `npm run typecheck`, and `npm run lint`. Skipped commit/push because `/Users/bank.p/Documents/Loop-Control-Plane` is not a Git repository. Analyzed 0 task images; none were associated with this checkbox.
